"""Package for the automodeling data pipeline logic.

It contains the following sub-packages:
    * :pack:`dags`: package containing the airflow DAGS definitions for the pipeline.
    * :pack:`tasks`: package containing the definition of all the tasks present in the DAGS.
    * :pack:`utils`: package containing all the generic code and utils used to generate
        the tasks and DAGS.
"""
